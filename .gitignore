# Personal/PII vault content — never version control (removed from git
# history 2026-08-23 after being committed by mistake).
vault/01-인수인계/
vault/07-경력-이력서/
vault/_원본백업/

# Kubeconfigs contain live cluster credentials.
*.kubeconfig

# Personal deploy-time values (real cluster hostnames/domains) — use
# values-example.yaml as the public template instead.
charts/vaultviewer/values-cluster-mesh1.yaml

# Node
web/node_modules/
web/dist/

# Go
*.test
/vaultviewer
/mcp-server
/vv-mcp
/bin/

# Local MCP server registration — carries a real (test) LDAP password in
# its env block, so it's never committed. See README's "AI agent 연동"
# section for the template.
/.mcp.json

# OS
.DS_Store
