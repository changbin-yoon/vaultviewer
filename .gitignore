# Personal/PII vault content — never version control (PII subdirectories
# removed from git history 2026-08-23 after being committed by mistake; the
# whole directory untracked 2026-09-06 since it holds real work notes that
# were only ever demo data. Files stay on disk — point VAULT_LOCAL_ROOT at
# it for local dev, or at examples/vault-seed/ for a clean sample vault).
vault/

# Kubeconfigs contain live cluster credentials.
*.kubeconfig

# Personal deploy-time values (real cluster hostnames/domains) — use
# values-example.yaml as the public template instead.
charts/vaultviewer/values-cluster-mesh1.yaml

# Node
web/node_modules/
web/dist/

# Go
*.test
/vaultviewer
/mcp-server
/vv-mcp
/bin/

# Local MCP server registration — carries a real (test) LDAP password in
# its env block, so it's never committed. See README's "AI agent 연동"
# section for the template.
/.mcp.json

# OS
.DS_Store
